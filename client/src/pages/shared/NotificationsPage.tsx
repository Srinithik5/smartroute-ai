import { PlaceholderPage } from '@/components/layout/PlaceholderPage';

export function NotificationsPage() {
  return (
    <PlaceholderPage
      title="Notifications"
      description="Delivery, dispatch, and fleet alerts will appear here once notification events are wired up in a future phase."
    />
  );
}