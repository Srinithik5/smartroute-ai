import { PlaceholderPage } from '@/components/layout/PlaceholderPage';

export function ActiveOrdersPage() {
  return (
    <PlaceholderPage
      title="Active Orders"
      description="Orders currently in progress through the delivery lifecycle will be implemented in a future phase."
    />
  );
}