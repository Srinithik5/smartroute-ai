import { PlaceholderPage } from '@/components/layout/PlaceholderPage';

export function CreateDeliveryPage() {
  return (
    <PlaceholderPage
      title="Create Delivery Request"
      description="The multi-section delivery request form (pickup, package, and review) will be implemented in a future phase."
    />
  );
}